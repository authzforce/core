<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.thalesgroup.authzforce</groupId>
		<artifactId>authzforce-parent</artifactId>
		<version>3.0.10</version>
	</parent>
	<artifactId>authzforce-core</artifactId>
	<version>3.5.9-SNAPSHOT</version>
	<name>AuthZForce :: Core :: XACML Core Implementation</name>
	<organization>
		<name>Thales Services SAS</name>
	</organization>
	<description>XACML-compliant Authorization PDP Core Engine</description>
	<inceptionYear>2011</inceptionYear>
	<scm>
		<!-- Used by Jenkins - Maven release plugin -->
		<connection>scm:git:${scm.baseUrl}/core.git</connection>
		<developerConnection>scm:git:${scm.baseUrl}/core.git</developerConnection>
		<tag>HEAD</tag>
		<!-- Publicly browsable repository URL. For example, via Gitlab web UI. -->
		<url>${scm.baseUrl}/core</url>
	</scm>
	<dependencies>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>jcl-over-slf4j</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.ws</groupId>
			<artifactId>spring-xml</artifactId>
		</dependency>
		<dependency>
			<!-- For loading XML schemas with OASIS catalog (CatalogManager) -->
			<groupId>xml-resolver</groupId>
			<artifactId>xml-resolver</artifactId>
			<version>1.2</version>
		</dependency>
		<dependency>
			<!-- For validation of XACML RFC822Name (email address) -->
			<groupId>com.sun.mail</groupId>
			<artifactId>javax.mail</artifactId>
			<version>1.5.4</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
		</dependency>
		<dependency>
			<!-- For validating IP addresses (XACML IPAdress datatype), Domain names (XACML DNSName datatype), 
				etc. without any DNS resolution -->
			<groupId>com.google.guava</groupId>
			<artifactId>guava</artifactId>
			<version>18.0</version>
		</dependency>
		<dependency>
			<!-- For XACML AttributeSelector evaluation and XPath-based functions (making reference to [XF]) -->
			<groupId>net.sf.saxon</groupId>
			<artifactId>Saxon-HE</artifactId>
			<version>9.6.0-5</version>
		</dependency>
		<!-- Authzforce dependencies -->
		<!-- In theory, no need to declare xml-ns-model and xacml-model dependencies since they are dragged 
			by authzforce-pdp-ext-model down below already. In practive, they are declared here only to work around 
			maven-jaxb2-plugin bug: cannot resolve episodes from indirect dependencies with useDependenciesAsEpisodes 
			option. -->
		<dependency>
			<groupId>com.thalesgroup.authzforce</groupId>
			<artifactId>authzforce-xml-ns-model</artifactId>
		</dependency>
		<dependency>
			<groupId>com.thalesgroup.authzforce</groupId>
			<artifactId>authzforce-xacml-model</artifactId>
		</dependency>
		<dependency>
			<groupId>com.thalesgroup.authzforce</groupId>
			<artifactId>authzforce-pdp-ext-model</artifactId>
		</dependency>
		<!-- /Authzforce dependencies -->

		<!-- Test dependencies -->
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.11</version>
			<scope>test</scope>
		</dependency>
		<!-- /Test dependencies -->
	</dependencies>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-pmd-plugin</artifactId>
				<version>3.5</version>
				<configuration>
					<targetJdk>1.7</targetJdk>
					<excludeRoots>
						<excludeRoot>target/generated-sources</excludeRoot>
						<excludeRoot>target/generated-test-sources</excludeRoot>
					</excludeRoots>
				</configuration>
				<executions>
					<execution>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>findbugs-maven-plugin</artifactId>
				<version>3.0.1</version>
				<executions>
					<execution>
						<phase>verify</phase>
						<goals>
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<!-- Apache license Headers -->
				<groupId>com.mycila</groupId>
				<artifactId>license-maven-plugin</artifactId>
				<executions>
					<execution>
						<id>add-gplv3-license</id>
						<phase>process-sources</phase>
						<configuration>
							<!-- Template location -->
							<header>license/thales-gpl.txt</header>
							<properties>
								<!-- Values to be substituted in template -->
								<inceptionYear>${project.inceptionYear}</inceptionYear>
								<currentYear>${currentYear}</currentYear>
								<copyrightOwner>${project.organization.name}</copyrightOwner>
								<projectName>AuthZForce</projectName>
							</properties>
							<strictCheck>true</strictCheck>
							<excludes>
								<exclude>**/*.html</exclude>
								<exclude>**/*.xml</exclude>
								<exclude>**/*.txt</exclude>
								<exclude>**/*.ec</exclude>
								<exclude>**/*.log</exclude>
								<exclude>**/*.css</exclude>
								<exclude>**/*.js</exclude>
								<exclude>**/*.jsp</exclude>
								<exclude>**/*.md</exclude>
								<exclude>**/*.properties</exclude>
								<exclude>**/*.gitignore</exclude>
							</excludes>
							<includes>
								<include>src/main/java/com/thalesgroup/authzforce/core/**</include>
								<!-- Include test files also -->
								<include>src/test/java/**</include>
							</includes>
						</configuration>
						<goals>
							<goal>format</goal>
						</goals>
					</execution>
					<execution>
						<id>add-gpl-license</id>
						<phase>process-sources</phase>
						<configuration>
							<!-- Template location -->
							<header>license/thales-gpl.txt</header>
							<properties>
								<!-- Values to be substituted in template -->
								<inceptionYear>${project.inceptionYear}</inceptionYear>
								<currentYear>${currentYear}</currentYear>
								<copyrightOwner>${project.organization.name}</copyrightOwner>
								<projectName>AuthZForce</projectName>
							</properties>
							<strictCheck>true</strictCheck>
							<includes>
								<include>src/main/java/com/thalesgroup/authzforce/core/**</include>
							</includes>
						</configuration>
						<goals>
							<goal>format</goal>
						</goals>
					</execution>
					<execution>
						<id>check-license</id>
						<goals>
							<!-- attached to verify phase -->
							<goal>check</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.jvnet.jaxb2.maven2</groupId>
				<artifactId>maven-jaxb2-plugin</artifactId>
				<configuration>
					<!-- debug=true will generate JAXBDebug class. More info: https://github.com/highsource/maven-jaxb2-plugin/wiki/Miscellaneous -->
					<debug>false</debug>
					<verbose>true</verbose>
					<extension>true</extension>
					<useDependenciesAsEpisodes>true</useDependenciesAsEpisodes>
					<strict>false</strict>
					<removeOldOutput>true</removeOldOutput>
					<catalog>src/main/jaxb/catalog.xml</catalog>
					<bindingDirectory>src/main/jaxb</bindingDirectory>
					<bindingIncludes>
						<include>bindings.xjb</include>
					</bindingIncludes>
				</configuration>
				<executions>
					<execution>
						<id>jaxb-generate-compile-sources</id>
						<goals>
							<goal>generate</goal>
						</goals>
						<configuration>
							<schemaDirectory>src/main/resources</schemaDirectory>
							<generateDirectory>${project.build.directory}/generated-sources/xjc</generateDirectory>
							<addCompileSourceRoot>true</addCompileSourceRoot>
							<addTestCompileSourceRoot>false</addTestCompileSourceRoot>
						</configuration>
					</execution>
					<execution>
						<id>jaxb-generate-test-sources</id>
						<goals>
							<goal>generate</goal>
						</goals>
						<configuration>
							<schemaDirectory>src/test/resources</schemaDirectory>
							<generateDirectory>${project.build.directory}/generated-test-sources/xjc</generateDirectory>
							<addCompileSourceRoot>false</addCompileSourceRoot>
							<addTestCompileSourceRoot>true</addTestCompileSourceRoot>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<!-- Test configuration -->
			<plugin>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.16</version>
				<executions>
					<execution>
						<id>default-test</id>
						<phase>test</phase>
						<goals>
							<goal>test</goal>
						</goals>
						<configuration>
							<systemPropertyVariables>
								<!-- Use random address instead of the host MAC address to generate UUIDs as this test may be 
									run on host disconnected from the network. -->
								<com.thalesgroup.theresis.useRandomAddressBasedUUID>true</com.thalesgroup.theresis.useRandomAddressBasedUUID>
							</systemPropertyVariables>
							<failIfNoTests>false</failIfNoTests>
							<skipTests>false</skipTests>
							<includes>
								<include>**/MainTest.java</include>
							</includes>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<executions>
					<execution>
						<id>attach-sources</id>
						<phase>verify</phase>
						<goals>
							<goal>jar-no-fork</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
